import React from "react";
import NavBar from "./NavBar";

function Header() {
  return (
    <header>
      <section>
        <NavBar />
      </section>
    </header>
  );
}

export default Header;
